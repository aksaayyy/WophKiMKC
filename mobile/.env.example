# API Configuration
# 
# For iOS Simulator: localhost works fine
# For Android USB device: use your computer's IP (e.g., 192.168.1.xxx)
# For physical devices: find IP with `ifconfig` (macOS) or `ipconfig` (Windows)

API_BASE_URL=http://localhost:3001/api

# Production
# API_BASE_URL=https://your-api-domain.com/api

# Android USB Example:
# API_BASE_URL=http://192.168.1.100:3001/api
