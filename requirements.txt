# Core dependencies
moviepy>=1.0.3
numpy>=1.20.0
opencv-python>=4.5.0
# Upgrade Pillow to a secure, modern release to mitigate several critical CVEs
# See project security advisories; require at least 10.3.0 or newer
Pillow>=10.3.0
tqdm>=4.50.0
yt-dlp>=2024.8.6

# Audio and signal processing
librosa>=0.9.0
scipy>=1.7.0
scikit-learn>=1.0.0
pydub>=0.25.1

# Optional dependencies (for captions and watch mode)
openai-whisper>=1.0.0  # For speech-to-text captions
watchdog>=2.0.0       # For folder watching functionality

# Web server dependencies
flask>=2.0.0
flask-cors>=3.0.0

# Development dependencies
pytest>=6.0.0
black>=21.0
isort>=5.0.0
mypy>=0.800