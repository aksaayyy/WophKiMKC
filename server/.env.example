# Server
PORT=3001
FRONTEND_URL=http://localhost:3000

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# YouTube OAuth (for uploading clips to YouTube)
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REDIRECT_URI=http://localhost:3001/api/oauth/callback
# Comma-separated refresh tokens for multi-account round-robin
YOUTUBE_REFRESH_TOKENS=

# Processing
DEFAULT_CLIP_COUNT=3
DEFAULT_CLIP_DURATION=40
UPLOAD_DELAY_MS=5000
WORKER_CONCURRENCY=2

# Admin Panel
ADMIN_PASSWORD=

# File cleanup (auto-delete old downloads/output)
CLEANUP_INTERVAL_HOURS=14

# Debug
DEBUG=false

# Aria2c (faster downloads)
ARIA2C_ENABLED=true
ARIA2C_CONNECTIONS=16

# Faster-Whisper (transcription)
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
PYTHON_PATH=python3

# LLM for smart hook detection
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=
